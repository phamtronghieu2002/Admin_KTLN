import { FC } from "react";
import "./overlay.scss"
interface OverlayProps {
    
}
 
const Overlay: FC<OverlayProps> = ({}) => {
    return (  
        <div id="overlay"></div>
    );
}
 
export default Overlay;